spring:
  application:
    name: hr-user
  profiles:
    active: test
  cloud:
    config:
      uri: http://localhost:8888
      label: main
server:
  port: ${PORT:0}
management:
  endpoints:
    web:
      exposure:
        include: '*'